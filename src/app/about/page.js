import { Typography } from "@mui/material";

const About = () => {
    return (
        <Typography variant="h4" color="text.secondary">
            Hello from About
        </Typography>
    );
};

export default About;