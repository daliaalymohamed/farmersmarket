import { Typography } from "@mui/material";

const Profile = () => {
    return (
        <Typography variant="h4" color="text.secondary">
            Hello from Profile
        </Typography>
    );
};

export default Profile;